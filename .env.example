# Client ID for the SMART on FHIR Sandbox (e.g., Logica/Meld)
# Register your app in the sandbox to get this ID.
NEXT_PUBLIC_MELD_CLIENT_ID=your_client_id_here

# Base URL for the application (default for local dev)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
